import { PlaceholderPage } from "@/components/placeholder-page";
import { getMessages } from "@/i18n/messages";

export default function CampaignsPage() {
  return <PlaceholderPage title={getMessages().placeholders.campaignsTitle} />;
}
