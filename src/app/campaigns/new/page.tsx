import { PlaceholderPage } from "@/components/placeholder-page";
import { getMessages } from "@/i18n/messages";

export default function NewCampaignPage() {
  return <PlaceholderPage title={getMessages().home.newCampaign} />;
}
