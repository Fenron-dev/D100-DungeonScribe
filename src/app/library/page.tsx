import { PlaceholderPage } from "@/components/placeholder-page";
import { getMessages } from "@/i18n/messages";

export default function LibraryPage() {
  return <PlaceholderPage title={getMessages().placeholders.libraryTitle} />;
}
