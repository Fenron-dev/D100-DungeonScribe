import { PlaceholderPage } from "@/components/placeholder-page";
import { getMessages } from "@/i18n/messages";

export default function SettingsPage() {
  return <PlaceholderPage title={getMessages().placeholders.settingsTitle} />;
}
